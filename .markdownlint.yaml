# Markdownlint config for Claude Code plugin skills
# Skills contain prose, tables, and CLI examples that naturally exceed 80 chars
MD013: false
# SKILL.md files start with YAML frontmatter, not a heading
MD041: false
